# multiview package

- `benchmark/`: task orchestration, triplets, metrics, results (thin wrappers)
- `eval/`: scoring methods (embeddings, bm25, lm_judge*)
- `docsets/`: dataset loaders + registry (`DOCSETS`)
- `inference/`: provider clients, caching, presets, prompting/parsing
- `utils/`: small shared helpers
