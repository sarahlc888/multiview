Given an anchor document, please find a document that is similar based on the specified criteria.

CRITERIA:
{criterion}

CRITERIA DESCRIPTION:
{criterion_description}

INSTRUCTIONS:
- Select a document similar to the anchor, based on the criterion
- Avoid choosing positives with very high lexical overlap; when tied on criteria, prefer candidates with lower spurious/lexical similarity.
- NOTE: Each candidate is annotated with  "Criterion" (similarity on what matters), "Spurious" (similarity on surface features), and "Lexical" (BM25 word overlap)
- IMPORTANT: Annotations are noisy. Always verify against actual document content.
{triplet_example_section}
ANCHOR DOCUMENT:
{anchor_doc}

ANCHOR ANNOTATION:
{anchor_annotation}

CANDIDATES:
{candidates}

Think out loud, then respond with valid JSON in this exact format:
{
  "reasoning": <thinking>,
  "chosen_positive": [number]
}

IMPORTANT: make sure to response in JSON format.
